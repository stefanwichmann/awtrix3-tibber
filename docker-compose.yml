services:
  awtrix-light-tibber:
    image: stefanwichmann/awtrix3-tibber
    restart: unless-stopped
    environment:
      - TZ=Europe/Berlin
      - TIBBER_TOKEN=insert_your_API_token_here
      - AWTRIX_IP=127.0.0.1
